# Dev: points to the Docker Caddy assets container (direct port, no hosts-file entry needed).
# The queue service docker-compose.yml maps 127.0.0.1:8080 → assets:80.
VITE_CONFIG_BASE_URL=http://localhost:8080
